{
  "name": "wiki-ticket-sdd-ui",
  "private": true,
  "version": "0.0.0",
  "description": "Local read-only dashboard for any WikiTicket SDD (worklog) repo",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "start": "npm run -w server start --",
    "dev": "npm run -w server dev & npm run -w web dev",
    "build": "npm run -w web build && npm run -w server build",
    "typecheck": "npm run -w server typecheck && npm run -w web typecheck",
    "test": "npm run -w server test && npm run -w web test"
  }
}
