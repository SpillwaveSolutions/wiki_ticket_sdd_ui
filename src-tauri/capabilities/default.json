{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capabilities — dialog open only; no direct fs/shell from frontend",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
